{
  "name": "@content-engine/m3-section",
  "version": "1.0.0",
  "description": "M3-SectionGenerator module for detailed content generation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:contract": "jest --testPathPattern=contract",
    "test:unit": "jest --testPathPattern=unit",
    "lint": "eslint src/**/*.ts",
    "validate:schema": "ajv validate --all-errors --verbose schemas/*.schema.json"
  },
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "crypto": "^1.0.1",
    "katex": "^0.16.8",
    "mathjs": "^12.2.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}